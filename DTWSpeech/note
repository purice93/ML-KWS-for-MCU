dtw动态时间规整算法：
通过动态规划对不同长度的序列进行比对

在语音关键词识别中的应用：
以已知关键词语音作为模板，计算未知语音同模板的距离。

主要的几个问题：
    1、同一个关键词有多个模板：
        由于内存的限制，不可能存储每一个模板，所以这里需要保存一个最优的模板：
            a)直接通过训练，找出准确率最好的模板，即通过率最高的模板。
            b)构建模板的高斯分布，找到模板的最大概率（具体实施情况还未知）
    2、多个关键词问题：
        如果有多个关键词，首先每个关键词都需要存储一个模板，占用内存
        另外，关键词之间的信号相似度可能比字形相似度更小，从而误识别，
        比如：'关闭语音助手'、'打开语音助手'仅仅只有前两个字不相同，如果由于说话人表达方式差距太大，
        同一个人说不同的这两句话和不同的人说同一句话，第一个差异可能更小

一点想法：
dtw的算法原理有点类似于hmm，只是hmm可能更加具体精细。同样是找路径。
所以是否可以将dtw加在dnn之后，代替hmm

准确率可能过低

